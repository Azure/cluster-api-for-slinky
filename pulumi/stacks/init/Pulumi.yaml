name: ca4s-init
description: |
  First PKO-owned stack. The outer host-side Pulumi stack creates exactly this
  one Stack CR after installing PKO; this stack then reflexively creates
  control-plane and tenants/workload resources from inside the management
  cluster.
runtime:
  name: python
  options:
    virtualenv: ../../../.venv
main: __main__.py
